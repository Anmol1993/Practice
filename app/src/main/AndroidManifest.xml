<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anmolgulwani.myapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>


    <uses-permission-sdk-23 android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission-sdk-23 android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="Android.hardware.camera" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Counter" />
        <activity android:name=".Calci" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Progress_Bar" />
        <activity android:name=".exp1" />
        <activity android:name=".SeekBarTest" />
        <activity android:name=".RadioButtonTest" />
        <activity android:name=".AutoCompleteText" />
        <activity android:name=".SpinnerTest" />
        <activity android:name=".SpinnerfromJava" />
        <activity android:name=".SwitchTest" />
        <activity android:name=".Optiontest" />
        <activity android:name=".Popuptest" />
        <activity android:name=".ContextTest" />
        <activity
            android:name=".Login_test"
            android:label="@string/title_activity_login" />
        <activity android:name=".Welcome" />
        <activity android:name=".Buyapp" />
        <activity android:name=".bill" />
        <activity android:name=".TestAlert" />
        <activity android:name=".testmusic" />
        <activity android:name=".testdatabase" />
        <activity android:name=".Testfilehandling" />
        <activity android:name=".testcamera" />
        <activity android:name=".TestSensor" />
        <activity android:name=".TestSensorall" />
        <activity android:name=".Test02Sensor" />
        <activity android:name=".testdisco" />
        <activity android:name=".testgetnumber" />
        <activity android:name=".TestContentProvider" />
        <activity android:name=".testWifi" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>